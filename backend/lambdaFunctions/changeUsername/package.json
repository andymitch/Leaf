{
  "name": "change-username",
  "version": "1.0.0",
  "description": "Change Username Lambda Function",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r ../changeUsername.zip * && aws lambda update-function-code --function-name changeUsername --zip-file fileb://../changeUsername.zip"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^8.5.1",
    "pg": "^7.12.1"
  }
}
