{
  "name": "forgot-password",
  "version": "1.0.0",
  "description": "Forgot Password Lambda Function",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r ../forgotPassword.zip * && aws lambda update-function-code --function-name forgotPassword --zip-file fileb://../forgotPassword.zip"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^7.12.1"
  }
}
